version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - durationcheck
    - errchkjson
    - errorlint
    - makezero
    - misspell
    - nilerr
    - nilnil
    - staticcheck
    - thelper
    - unconvert
    - usetesting
    - wastedassign
